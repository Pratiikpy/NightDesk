# NightDesk Alpha Gateway — Submission Copy

## 200-word description

NightDesk is a **complete autonomous trading loop for Bitget tokenized US stocks — no human in the
middle.** Every night it perceives (live token + real-NYSE prices, news, macro), a Qwen-powered
multi-role council decides trade or stand-down, 15 deterministic gates enforce risk, BitSim executes
realistic fills, and every decision is graded at the NYSE open and Ed25519-signed. Traditional quant
can't do this.

The edge is structural, not a chart guess: tokenized stocks trade 24/7, but the real stock only prices
during NYSE hours, and the Bitget stock perp — a blended issuer index — co-moves with the token and
hides the off-hours dislocation. NightDesk anchors each token to the real NYSE price and surfaces the
true gap (live: ~17 of 19 tokens dislocated vs the perp's ~0), then fades only what it can cleanly
capture — behind a certificate firewall that blocks any unsafe agent trade before it executes.

It also runs an autonomous Alpha Factory (9,720 strategies, an Overfit Court, a frozen champion at
+54.93 USDT in-sample), a forward out-of-sample paper record, MCP tools + an SDK so other agents route
through it, and a signed, reproducible evidence pack. Honest by design: we tested our own edge and
report where it is null. A complete loop and the foundational safety layer other tokenized-stock
agents pass through — every number replayable.

## Project description — 4-part structure (for the submission form)

**1. The problem.** Bitget's tokenized US stocks trade 24/7, but the real stock only prices during NYSE
hours. Off-hours they drift from fair value on thin, broker-quoted liquidity retail can't arbitrage,
and the Bitget stock perp — a blended issuer index — co-moves with the token and HIDES the true gap.
An agent trading these tokens off-hours is effectively blind, and the one reference it would reach for
is the one that conceals the dislocation.

**2. How it works (the autonomous loop, no human in the middle).** Perceive (live token + real-NYSE
anchor + news + macro) → Decide (a Qwen multi-role council debates the gap, or stands down on real
news) → Gate (15 hard risk gates) → Execute (BitSim realistic fills) → Grade (at the NYSE open;
win/loss written back to memory). Every proposed trade — from NightDesk's own loop or any external
agent — must first pass a signed certificate firewall (ALLOW / ALLOW-CAPPED / REJECT). Zero human
interventions, every decision Ed25519-signed.

**3. Evidence (every number replayable).** Live finding: ~17 of 19 tokens dislocated vs the real stock
while the perp shows ~0. Paper-trading logs (timestamp, asset, direction, price, quantity, balance
change). Autonomous Alpha Factory: 9,720 candidates, 58,320 trials, Overfit Court, frozen champion
+54.93 USDT (in-sample). Forward out-of-sample paper record. A real authenticated Bitget round-trip
(read-only key, verified on-platform). 215 tests; the whole pack regenerates from one command.

**4. My take on AI trading.** The biggest lie in AI trading is a lucky backtest dressed as a guaranteed
edge. We did the opposite — we tested the raw convergence edge honestly and report exactly where it stops
(49.6% corrective, a coin flip; and a Deflated Sharpe that, after correcting across 9,720 candidates and 58,320 session trials, is not yet
significant). The raw edge alone is not the product: NightDesk's value is turning noisy tokenized-stock
gaps into certified, gated, executable decisions. An honest agent that knows what it cannot predict,
stands down on real news, and enforces hard risk discipline is worth more than a confident gambler.

This is Trading Agent, Trading Infrastructure, and Stock AI Trading at once: a complete autonomous
loop, an agent firewall + certification layer, and a real-stock fair-value solution — all judged
together for #1.

## Track fit

- **Trading Infrastructure:** firewall, signed certificates, MCP tools, SDK, stress/gauntlet lab,
  Alpha Factory, Overfit Court, signed ledgers, reproducible judge pack.
- **US Stock AI Trading:** real-stock fair-value anchor for Bitget tokenized stocks, gap causality,
  token quality board, event-aware abstention, frozen champion paper trading evidence.
- **Trading Agent:** autonomous perception -> council -> gates -> simulated execution -> grading
  loop plus autonomous strategy search/freeze/expected-vs-actual reporting with no human
  intervention.

## Verifiable usage records

- `evidence/judge-run.json` — generated by `npm run evidence`.
- `evidence/api-call-log.jsonl` — SDK/HTTP/MCP/kernel firewall usage records.
- `evidence/trading-log/nightdesk-paper-trading-log.csv` — paper trading log with timestamp, asset,
  direction, price, quantity, balance change, certificate id, verdict, policy, reason, and ledger
  hash.
- `evidence/trading-log/run-summary.md` — paper session summary across the tokenized-stock universe.
- `evidence/trading-log/guarded-replay/guarded-replay-paper-trading-log.csv` — longer recorded-day
  guarded replay with realized entries/exits. Current run: 1,000.00 -> 1,004.34 USDT, 38 fills, and
  every blocked unsafe/non-executable intent logged.
- `evidence/trading-log/guarded-replay/threshold-search.json` — transparent threshold search; this
  is labeled as execution evidence, not an out-of-sample alpha claim.
- `evidence/alpha-championship/alpha-championship-report.md` — raw-PnL championship report over
  recorded sessions. Current single-session champion: 1,000.00 -> 1,034.61 USDT on the 2026-06-17
  recording; global same-config replay totals +54.93 USDT across available recordings.
- `evidence/championship/` — two-champion Championship Mode: `leaderboard_pnl.csv`,
  `leaderboard_safety.csv`, frozen PnL Champion, frozen Safety Champion, both paper logs, and
  `champion-overfit-check.md`. Current PnL Champion is separated from the Safety Champion so the
  project can compete on green-number PnL without pretending the production objective is pure risk.
- `evidence/championship/champion-overfit-card.md` — compact overfit card with configs/trials,
  same-sample PnL, leave-one-session/token checks, cost/slippage/threshold stress, max drawdown,
  profit concentration, and fragility label.
- `evidence/alpha-factory/manifest.json` — autonomous Alpha Factory manifest: 9,720 candidates,
  58,320 trials, 8,448 rejected configs, walk-forward leaderboard, frozen champion, live-paper log,
  expected-vs-actual report, masked-eval note, and agent benchmark standard. Current frozen
  champion: +54.93 USDT total on the recorded sessions with 6.33 USDT max drawdown — in-sample
  execution-quality evidence, not a future-alpha claim.
- `evidence/alpha-factory/live-paper-trading-log.csv` — Bitget-style paper log for the frozen
  champion with timestamp, asset, direction, price, quantity, balance change, strategy id, and
  safety status.
- `evidence/alpha-factory/alpha-zoo-catalog.csv` and `strategy-compare.csv` — NightDesk Alpha Zoo
  plus head-to-head strategy comparison, scoped to Bitget tokenized-stock gaps.
- `evidence/shadow-gateway/actual-vs-guarded.csv` — Shadow Account-style counterfactual report:
  unrestricted agent vs NightDesk-guarded path, missed profit, blocked loss, and rule breaks.
- `evidence/claims/claims-manifest.json` — claim ledger mapping every major claim to acceptance
  criteria and evidence.
- `evidence/run-cards/` — compact run cards for paper session, Alpha Factory, raw-PnL championship,
  guarded replay, Bitget smoke, and judge-max verification.
- `evidence/doctor-report.md`, `evidence/data-health/source-health.md`, and
  `evidence/judge-cockpit/index.html` — operational doctor, source-health matrix, and a static Judge
  Cockpit.
- `evidence/agent-arena-v2/` — seven external-agent policy runs, each with Bitget-style paper
  trading CSV/JSONL logs and an arena summary. This proves NightDesk can sit in front of other
  agents, not only its own loop.
- `evidence/research/` — train/test signal-stability research pack plus a walk-forward caveat so the
  replay PnL is presented honestly.
- `evidence/oos/` — multi-session study over every recorded snapshot file.
- `evidence/oos/session-bank/` — session cards, ledgers, and OOS quality scoreboard showing current
  progress toward 10/20/50 sessions while the background daemon collects more.
- `evidence/walkforward/` — leave-one-session-out PnL report, cost sweep, and regime summary.
- `evidence/fill-model/` — fill realism stress report covering empty/one-sided/crossed/wide/partial
  and stale-quote cases.
- `evidence/integration/` — SDK/MCP-style external-agent integration logs and sample I/O.
- `evidence/bitget-live/` — live public Bitget read-only proof, with no credentials and no write
  permissions.
- `docs/BITGET_NATIVE_PROOF.md` — first-page proof card for Bitget public data, Agent Hub
  compatibility, MCP proof, read-only mode, env-only credentials, and paper-log format.
- `docs/CLAIM_LEDGER.md` — every major claim mapped to evidence and reproduction command.
- `evidence/max-judge-manifest.json` — generated by `npm run judge:max`; verifies the complete
  judge-visible evidence pack exists.
- `evidence/scenario-uat/scenario-uat-report.md` — persona-based scenario test report covering a
  fresh judge, paper-log judge, external agent, malicious agent, quant/PnL judges, Bitget-native
  proof, ops/security, cockpit demo, offline mode, and the OOS daemon.
- `evidence/paper-log-verify.json` — strict Bitget paper-log schema and balance-delta verification.
- `evidence/ledger-tamper-test.json` — signed-ledger tamper test: mutation, deletion, reorder, and
  signature swap must fail verification.
- `evidence/gates/gate-coverage.md` — pass/fail coverage for all 15 hard gates.
- `evidence/redteam/redteam-report.md` — hostile intent suite proving malformed, stale, spoofed,
  non-finite, and policy-violating intents do not execute unsafely.
- `evidence/sample-inputs/` and `evidence/sample-outputs/` — concrete intents, verdicts, and signed
  certificate.
- `data/ledger/*.jsonl` plus `data/ledger/*.sig.json` — signed replayable decision ledger.
- `sdk/examples/external-agent.ts` — third-party agent integration example.

## Judge command checklist

```bash
npm install
npm run build
npm test
npm run evidence
npm run paper-session
npm run paper-replay
npm run arena:v2
npm run alpha:championship
npm run championship:search
npm run alpha:factory
npm run alpha:zoo
npm run shadow-gateway
npm run claims:verify
npm run run-card
npm run doctor
npm run data:health
npm run dashboard:judge
npm run research:node
npm run oos:report
npm run oos:session-bank
npm run pnl:walkforward
npm run fill:stress
npm run external:proof
npm run bitget:read-only-proof
npm run judge
npm run judge:max
npm run final:verify
npm run scenario:verify
npm run judge:max:full
npm run dashboard
npx tsx sdk/examples/external-agent.ts
npm run mcp
```

## 3-minute demo story

1. **Problem:** Tokenized US stocks trade all night, but the true stock anchor is closed. Agents can
   trade blind, and the perp can hide the real rToken gap.
2. **Truth gap:** Show the cockpit’s Bitget Native Proof and Safety Gateway panels: read-only
   Bitget data, external MCP proof, paper log, and ledger proof all show PASS.
3. **Alpha Factory:** Show `evidence/alpha-factory/manifest.json`: candidates, trials, rejected
   configs, frozen champion, expected-vs-actual log. This proves the agent searches for profit
   instead of hand-picking one strategy.
4. **Judge Cockpit:** Open `evidence/judge-cockpit/index.html`: one screen shows Alpha Factory,
   raw-PnL championship, two-champion Championship Mode, safety gateway, doctor, and data-health
   status.
5. **Risk desk:** Show `npm run dashboard`. Point to true gap vs NYSE anchor, perp gap, cause, and
   action.
6. **Gateway:** Show `evidence/sample-inputs/safe-buy-intent.json`, then
   `evidence/sample-outputs/safe-buy-verdict.json`. NightDesk returns ALLOW/ALLOW_CAPPED/REJECT.
7. **Unsafe path:** Show `unsafe-sell-verdict.json`: a rich/short or policy-violating intent is
   rejected before execution.
8. **Paper record:** Show `evidence/alpha-factory/live-paper-trading-log.csv` and
   `evidence/trading-log/guarded-replay/guarded-replay-paper-trading-log.csv`:
   timestamp, asset, direction, price, quantity, balance change, certificate, verdict, policy,
   reason, ledger hash. The raw-PnL layer includes a PnL Champion for the green-number case and a
   Safety Champion for the production case; both are frozen separately and exported with paper logs.
9. **Overfit proof:** Show `evidence/championship/champion-overfit-card.md`: it labels same-sample
   PnL honestly and reports stress checks instead of hiding fragility.
10. **OOS proof:** Show `evidence/oos/session-bank/session-quality-report.md`: current sessions are
   signed and future sessions append through the running daemon.
11. **Shadow proof:** Show `evidence/shadow-gateway/actual-vs-guarded.csv`: it reports missed profit
   and blocked loss instead of hiding uncomfortable counterfactuals.
12. **Fast paper proof:** Show `evidence/trading-log/nightdesk-paper-trading-log.csv`: timestamp, asset,
   direction, price, quantity, balance change, certificate, verdict, policy, reason, ledger hash.
13. **Proof:** Show `npm run judge:max`: tests pass, evidence artifacts verify, signed ledger exists,
   paper logs exist, alpha artifacts exist, and the max evidence manifest verifies. Mention that
   `npm run judge:max:full` regenerates the full stack including Alpha Championship and Alpha
   Factory.
14. **Integration:** Show `sdk/examples/external-agent.ts` and MCP `evaluate_intent`. The product is
   not only an agent; it is the gateway other agents pass through.

## One-line pitch

NightDesk is the alpha lab that finds tokenized-stock trades and the safety gateway that decides
whether an agent is allowed to execute them.
